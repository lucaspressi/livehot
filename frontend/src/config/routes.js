/**
 * Application route definitions for React Router.
 */
const ROUTES = {
  HOME: '/',
  LOGIN: '/login',
  REGISTER: '/register',
  BROADCAST: '/broadcast/:id',
  WALLET: '/wallet',
  PROFILE: '/profile',
  STREAM: '/streams/:id',
};

export default ROUTES;
